name(data[,ncol(data)]) <- "Survival"
colnames(data[,ncol(data)]) <- "Survival"
names(data[,ncol(data)]) <- "Survival"
View(data)
names(data[,ncol(data)])
names(data[12])
names(data[12]) <- "Survival"
View(data)
colnames(data[12]) <- "Survival"
View(data)
colnames(data[11]) <- "Survival"
View(data)
# Feature amplification
# Survival
data <- cbind(data, matrix(data$Last_Year > 1977, colnames="Survival"))
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
View(data)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Feature amplification
# Survival
# Feature amplification
# Survival
colnames(data[1])
# Feature amplification
# Survival
colnames(data[11])
# Feature amplification
# Survival
colnames(data[12])
# Feature amplification
# Survival
data <- cbind(data, data$Last_Year > 1977)
colnames(data[12])
colnames(data[12]) <- "Survival"
colnames(data[12])
colnames(data[12]) <- c("Survival")
colnames(data[12])
colnames(data[12]) <- c('Survival')
colnames(data[12])
colnames(data[12]) <-´Survival´
# Feature amplification
# Survival
data <- cbind(data, as.data.frame(c(data$Last_Year > 1977), colnames = "Survival")
colnames(data[12]) <-
colnames(data[12])
# Feature amplification
# Survival
data <- cbind(data, as.data.frame(c(data$Last_Year > 1977), col.names = "Survival"))
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
data <- cbind(data, as.data.frame(c(data$Last_Year > 1977), col.names = "Survival"))
colnames(data[12]) <-
colnames(data[12])
View(data)
# Feature amplification
# Survival
survival = data$Last_Year > 1977
cbind(data, Survival)
# Feature amplification
# Survival
Survival = data$Last_Year > 1977
data <-cbind(data, Survival)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = data$Last_Year > 1977
data <-cbind(data, Survival)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = finches$Last_Year > 1977
data <-cbind(finches, Survival)
dmy <- dummyVars(" ~ .", data = finches)
trsf <- data.frame(predict(dmy, newdata = finches))
Age <- finches$Last_Year - finches$First_adult_year
Age
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = finches$Last_Year > 1977
Age <- finches$Last_Year - finches$First_adult_year
data <-cbind(finches, Survival, Age)
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = finches$Last_Year > 1977
Age <- finches$Last_Year - finches$First_adult_year
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
data <-cbind(finches, Survival, Age, Beak_volume)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
Survival = as.integer(as.logical(finches$Last_Year > 1977))
Age <- finches$Last_Year - finches$First_adult_year
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
data <-cbind(finches, Survival, Age, Beak_volume)
df[df$Sex=="male"]<-2
df[df=="male"]<-2
finches$Sex
finches$Sex == "male"
hist(finches$Sex)
barplot(finches$Sex)
barplot(c(finches$Sex)
barplot(c(finches$Sex))
barplot(c(finches$Sex))
View(finches)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
Survival = as.integer(as.logical(finches$Last_Year > 1977))
Age <- finches$Last_Year - finches$First_adult_year
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
finches <-cbind(finches, Survival, Age, Beak_volume)
barplot(c(finches$Sex))
barplot(table(finches$Sex))
barplot(table(finches$Sex), ylim=50)
barplot(table(finches$Sex))
hist(finches)
hist(finches$Band)
hist(finches$Age)
plot(finches$Age)
hist(finches$Beak_volume)
barplot(table(finches$Survival))
colnames(finches)
c(colnames(finches))
hist(finches$Age)
quant_data <- c("Weight_.g.", "Wing_.mm.", "Tarsus_.mm.", "Beak_Length_.mm.", "Beak_Depth_.mm.", "Beak_Width_.mm.", "Beak_volume")
for (i in quant_data){
plot(finches$i)
}
finches$quant_data[1]
finches[1]
source('~/github/masters/module_ii/statistical_inference/assignment/test.R')
quant_col <- range(6,12)
quant_col
quant_col <- range(6:12)
quant_col
quant_col <- range(6:12)
quant_col
quant_col <- c(range(6:12))
quant_col
quant_col <- c(6:12)
quant_col
for (i in quant_col){
plot(finches$i)
}
for (i in quant_col){
plot(finches[i])
}
for (i in quant_col){
hist(finches[i])
}
hist(finches$Weight_.g.)
hist(finches[i])
for (i in quant_col){
hist(finches[,i])
}
for (i in quant_col){
hist(finches[,i], main=colnames(finches[i]))
}
quant_col <- c(6:13)
for (i in quant_col){
hist(finches[,i], main=colnames(finches[i]))
}
source('~/github/masters/module_ii/statistical_inference/assignment/test.R')
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
shapiro.test(finches[,i])
}
quant_col <- c(6:13)
norm_results <- c()
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
norm_results.append(shapiro.test(finches[,i]))
}
norm_results <- c()
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
append(norm_results, shapiro.test(finches[,i]))
}
print(norm_results)
norm_results <- c()
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
print(shapiro.test(finches[,i]))
}
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year
Survival = as.integer(as.logical(finches$Last_Year > 1977))
finches <-cbind(finches, Beak_volume, Age, Survival)
#Plot
barplot(table(finches$Sex))
quant_col <- c(6:13)
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
}
#Plot
barplot(table(finches$Sex))
quant_col <- c(6:13)
for (i in quant_col){
print(colnames(finches[i]))
plot(density(finches[,i]), main=colnames(finches[i]))
}
#Correlations
cors <- cor(finches)
#Correlations
cors <- cor(finches[quant_col])
View(cors)
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year +1
Survival = as.integer(as.logical(finches$Last_Year > 1977))
finches <-cbind(finches, Beak_volume, Age, Survival)
#Plot
barplot(table(finches$Sex))
quant_col <- c(6:13)
for (i in quant_col){
print(colnames(finches[i]))
plot(density(finches[,i]), main=colnames(finches[i]))
}
#Correlations
cors <- cor(finches[quant_col])
View(cors)
View(finches)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
finches$Sex[finches$Sex == "unknown"] <- NA
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year +1
Survival = as.integer(as.logical(finches$Last_Year > 1977))
finches <-cbind(finches, Beak_volume, Age, Survival)
#Plot
barplot(table(finches$Sex))
quant_col <- c(6:13)
for (i in quant_col){
print(colnames(finches[i]))
plot(density(finches[,i]), main=colnames(finches[i]))
}
#Correlations
cors <- cor(finches[quant_col])
#Correlations
cors <- cor(finches[quant_col])
cors_noNA <- cor(finches[quant_col], use="complete.obs")
View(cors_noNA)
describe(finches)
summary(finches)
factor(finches$Sex)
finches <- data.frame/finches
finches <- data.frame(finches)
#Plot
barplot(table(finches$Sex))
table(finches$Sex)
table(finches$Sex)[female]
table(finches$Sex)["female"]
#Plot
barplot(table(finches$Sex["female", "male"]))
#Plot
barplot(table(finches$Sex[c("female", "male")]))
table(finches$Sex[c("female", "male")])
#Plot
barplot(table(finches$Sex)[c("female", "male")])
#Plot
barplot(table(finches$Sex)[c("female", "male")], height=50)
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=50)
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=(0,50))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=(red, blue))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=("red", "blue"))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=c("red", "blue"))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=c("pink", "gold"))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=c("pink", "darkblue"))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=c("purple", "darkblue"))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=c("purple", "cyan"))
#Plot
barplot(table(finches$Sex)[c("female", "male")], ylim=c(0,50), ylab="Number of specimens", col=c("purple", "gold"))
# Check which variables are in the dataset
colnames(finches)
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Replace the unknown values for Sex with NA.
finches$Sex[finches$Sex == "unknown"] <- NA
# Check which variables are in the dataset
colnames(finches)
summary(finches)
levels(finches$Sex)
levels(finches$Sex) <- c("male", "female")
factor(finches$Sex)
levels(finches$Sex)
droplevels(finches$Sex)
levels(finches$Sex)
finches$Sex <- droplevels(finches$Sex)
levels(finches$Sex)
#Plot
barplot(table(finches$Sex), ylim=c(0,50), ylab="Number of specimens", col=c("purple", "gold"))
# Check which variables are in the dataset
length(colnames(finches))
# Check which variables are in the dataset
colnames(finches)
# Summary of those variables
for (i in c(3:length(colnames(finches)))){
print(summary(finches[,i]))
}
# Summary of those variables
for (i in c(3:length(
colnames(finches)))){
summary(finches[,i])
}
# Summary of those variables
summary(finches[,c(3:11)])
# Summary of those variables
summary(finches[,c(3:11)])
# Summary of those variables
summary(finches[,c(3:11)])
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year + 1
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year + 1
#Survival = as.integer(as.logical(finches$Last_Year > 1977))
Survival = finches$Last_Year > 1977
finches <-cbind(finches, Beak_volume, Age, Survival)
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year + 1
#Survival = as.integer(as.logical(finches$Last_Year > 1977))
Survival = finches$Last_Year > 1977
finches <-cbind(finches, Beak_volume, Age, Survival)
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Replace the unknown values for Sex with NA.
finches$Sex[finches$Sex == "unknown"] <- NA
finches$Sex <- droplevels(finches$Sex)
# Check which variables are in the dataset
colnames(finches)
# Feature amplification
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year + 1
#Survival = as.integer(as.logical(finches$Last_Year > 1977))
Survival = finches$Last_Year > 1977
finches <-cbind(finches, Beak_volume, Age, Survival)
# Summary of those variables
summary(finches[,c(3:ncol(finches))])
pairs(finches)
# Summary of those variables
summary(finches[,c(3:ncol(finches))])
var(finches[,c(3:ncol(finches))])
sd(finches[,2:c(3:ncol(finches))-1])
sd(finches[,c(4:ncol(finches)-1)])
var(finches[,c(4:ncol(finches)-1)])
var(finches[,c(3:ncol(finches)-1)])
var(finches[,c(2:ncol(finches)-1)])
var(finches[,c(1:ncol(finches)-1)])
var(finches[,c(5:ncol(finches)-1)])
var(finches[,c(4:ncol(finches)-1)])
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Replace the unknown values for Sex with NA.
finches$Sex[finches$Sex == "unknown"] <- NA
finches$Sex <- droplevels(finches$Sex)
# Check which variables are in the dataset
colnames(finches)
# Feature amplification
#Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
Age <- finches$Last_Year - finches$First_adult_year + 1
#Survival = as.integer(as.logical(finches$Last_Year > 1977))
Survival = finches$Last_Year > 1977
finches <-cbind(finches, Age, Survival)
# Summary of those variables
summary(finches[,c(3:ncol(finches))])
var(finches[,c(4:ncol(finches)-1)])
sd(finches[,c(4:ncol(finches)-1)])
var(finches[,c(4:(ncol(finches)-1))])
sd(finches[,c(4:(ncol(finches)-1))])
sd(finches$Age)
sd(finches[,11])
# Summary of those variables
for (i in c(3:ncol(finches))){
print(colnames(finches[,i]))
summary(finches[,i])
print(paste("Var:",var(finches[,i])))
print(paste("SD:",sd(finches[,i])))
}
# Summary of those variables
for (i in c(3:ncol(finches))){
print(colnames(finches[,i]))
summary(finches[,i])
if (colnames(finches[,i]) != "Sex"){
print(paste("Var:",var(finches[,i])))
print(paste("SD:",sd(finches[,i])))
}
}
# Summary of those variables
for (i in c(3:ncol(finches))){
print(colnames(finches[,i]))
print(summary(finches[,i]))
}
print(names(finches[,i]))
# Summary of those variables
for (i in c(3:ncol(finches))){
print(names(finches[,i]))
print(summary(finches[,i]))
}
print(finches[,i])
# Summary of those variables
for (i in c(3:ncol(finches))){
print(finches[,i])
print(summary(finches[,i]))
}
print(colnames(finches)[i])
# Summary of those variables
for (i in c(3:ncol(finches))){
print(colnames(finches)[i])
print(summary(finches[,i]))
if (colnames(finches)[i] != "Sex"){
print(paste("Var:",var(finches[,i])))
print(paste("SD:",sd(finches[,i])))
}
}
# Summary of those variables
for (i in c(3:ncol(finches))){
print(colnames(finches)[i])
print(summary(finches[,i]))
if (colnames(finches)[i] != "Sex" | colnames(finches)[i] != "Survival"){
print(paste("Var:",var(finches[,i])))
print(paste("SD:",sd(finches[,i])))
}
}
summary(finches[,c(3:ncol(finches))])
# Summary of those variables
for (i in c(3:ncol(finches))){
print(colnames(finches)[i])
print(summary(finches[,i]))
if (colnames(finches)[i] != "Sex" & colnames(finches)[i] != "Survival"){
print(paste("Var:",var(finches[,i])))
print(paste("SD:",sd(finches[,i])))
}
}
#Plot
barplot(table(finches$Sex), ylim=c(0,50), ylab="Number of specimens", col=c("purple", "gold"))
quant_col <- c(6:13)
for (i in quant_col){
print(colnames(finches)[i])
plot(density(finches[,i]), main=colnames(finches[i]))
}
quant_col <- c(6:12)
for (i in quant_col){
print(colnames(finches)[i])
plot(density(finches[,i]), main=colnames(finches[i]))
}
for (i in quant_col){
print(colnames(finches)[i])
hist(finches[,i]), main=colnames(finches[i])
lines(density(finches[,i]))
}
for (i in quant_col){
for (i in quant_col){
hist(finches[,i], main=colnames(finches[i])
lines(density(finches[,i]))
}
#Correlations
cors <- cor(finches[quant_col])
cors_noNA <- cor(finches[quant_col], use="complete.obs")
summary(finches)
for (i in quant_col){
}
for (i in quant_col){
print(colnames(finches)[i])
hist(finches[,i], main=colnames(finches[i]))
lines(density(finches[,i]))
}
for (i in quant_col){
print(colnames(finches)[i])
hist(finches[,i], main=colnames(finches[i]), prob=TRUE)
lines(density(finches[,i]))
}
