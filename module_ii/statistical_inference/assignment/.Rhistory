ls
pwd
wd
setwd
setwd("/home/xoel/github/masters/module_ii/statistical_inference")
data <- read.table("finches_dataset.txt", header=TRUE, sep="\t")
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
setwd("/home/xoel/github/masters/module_ii/statistical_inference")
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
data <- read.table("home/xoel/github/masters/module_ii/statistical_inference/finches_dataset.txt", header=TRUE, sep="\t")
data <- read.table("finches_dataset.txt", header=TRUE, sep="\t")
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
data <- read.table("./finches_dataset.txt", header=TRUE, sep="/t")
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
finches_dataset <- read.delim("~/github/masters/module_ii/statistical_inference/assignment/finches_dataset.txt")
View(finches_dataset)
data <- read.delim("./finches_dataset.txt")
View(data)
View(data)
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.delim("./finches_dataset.txt")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
View(data)
View(data)
# Feature amplification
# Survival
data <- cbind(data, data$Last_Year > 1977)
View(data)
colnames(data$`data$Last_Year > 1977`) <- "Survival"
name(data[,ncol(data)]) <- "Survival"
colnames(data[,ncol(data)]) <- "Survival"
names(data[,ncol(data)]) <- "Survival"
View(data)
names(data[,ncol(data)])
names(data[12])
names(data[12]) <- "Survival"
View(data)
colnames(data[12]) <- "Survival"
View(data)
colnames(data[11]) <- "Survival"
View(data)
# Feature amplification
# Survival
data <- cbind(data, matrix(data$Last_Year > 1977, colnames="Survival"))
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
View(data)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Feature amplification
# Survival
# Feature amplification
# Survival
colnames(data[1])
# Feature amplification
# Survival
colnames(data[11])
# Feature amplification
# Survival
colnames(data[12])
# Feature amplification
# Survival
data <- cbind(data, data$Last_Year > 1977)
colnames(data[12])
colnames(data[12]) <- "Survival"
colnames(data[12])
colnames(data[12]) <- c("Survival")
colnames(data[12])
colnames(data[12]) <- c('Survival')
colnames(data[12])
colnames(data[12]) <-´Survival´
# Feature amplification
# Survival
data <- cbind(data, as.data.frame(c(data$Last_Year > 1977), colnames = "Survival")
colnames(data[12]) <-
colnames(data[12])
# Feature amplification
# Survival
data <- cbind(data, as.data.frame(c(data$Last_Year > 1977), col.names = "Survival"))
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
data <- cbind(data, as.data.frame(c(data$Last_Year > 1977), col.names = "Survival"))
colnames(data[12]) <-
colnames(data[12])
View(data)
# Feature amplification
# Survival
survival = data$Last_Year > 1977
cbind(data, Survival)
# Feature amplification
# Survival
Survival = data$Last_Year > 1977
data <-cbind(data, Survival)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
data <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = data$Last_Year > 1977
data <-cbind(data, Survival)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = finches$Last_Year > 1977
data <-cbind(finches, Survival)
dmy <- dummyVars(" ~ .", data = finches)
trsf <- data.frame(predict(dmy, newdata = finches))
Age <- finches$Last_Year - finches$First_adult_year
Age
setwd("/home/xoel/github/masters/module_ii/statistical_inference/assignment")
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = finches$Last_Year > 1977
Age <- finches$Last_Year - finches$First_adult_year
data <-cbind(finches, Survival, Age)
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
# Feature amplification
# Survival
Survival = finches$Last_Year > 1977
Age <- finches$Last_Year - finches$First_adult_year
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
data <-cbind(finches, Survival, Age, Beak_volume)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
Survival = as.integer(as.logical(finches$Last_Year > 1977))
Age <- finches$Last_Year - finches$First_adult_year
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
data <-cbind(finches, Survival, Age, Beak_volume)
df[df$Sex=="male"]<-2
df[df=="male"]<-2
finches$Sex
finches$Sex == "male"
hist(finches$Sex)
barplot(finches$Sex)
barplot(c(finches$Sex)
barplot(c(finches$Sex))
barplot(c(finches$Sex))
View(finches)
# The dataset had to be fixed, since there was blank spaces instead of tabulations at different points across the file.
finches <- read.table("./finches_dataset.txt", header=TRUE, sep="\t")
Survival = as.integer(as.logical(finches$Last_Year > 1977))
Age <- finches$Last_Year - finches$First_adult_year
Beak_volume <- ((finches$Beak_Width_.mm./2)**2*pi*finches$Beak_Depth_.mm.)/3
finches <-cbind(finches, Survival, Age, Beak_volume)
barplot(c(finches$Sex))
barplot(table(finches$Sex))
barplot(table(finches$Sex), ylim=50)
barplot(table(finches$Sex))
hist(finches)
hist(finches$Band)
hist(finches$Age)
plot(finches$Age)
hist(finches$Beak_volume)
barplot(table(finches$Survival))
colnames(finches)
c(colnames(finches))
hist(finches$Age)
quant_data <- c("Weight_.g.", "Wing_.mm.", "Tarsus_.mm.", "Beak_Length_.mm.", "Beak_Depth_.mm.", "Beak_Width_.mm.", "Beak_volume")
for (i in quant_data){
plot(finches$i)
}
finches$quant_data[1]
finches[1]
source('~/github/masters/module_ii/statistical_inference/assignment/test.R')
quant_col <- range(6,12)
quant_col
quant_col <- range(6:12)
quant_col
quant_col <- range(6:12)
quant_col
quant_col <- c(range(6:12))
quant_col
quant_col <- c(6:12)
quant_col
for (i in quant_col){
plot(finches$i)
}
for (i in quant_col){
plot(finches[i])
}
for (i in quant_col){
hist(finches[i])
}
hist(finches$Weight_.g.)
hist(finches[i])
for (i in quant_col){
hist(finches[,i])
}
for (i in quant_col){
hist(finches[,i], main=colnames(finches[i]))
}
quant_col <- c(6:13)
for (i in quant_col){
hist(finches[,i], main=colnames(finches[i]))
}
source('~/github/masters/module_ii/statistical_inference/assignment/test.R')
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
shapiro.test(finches[,i])
}
quant_col <- c(6:13)
norm_results <- c()
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
norm_results.append(shapiro.test(finches[,i]))
}
norm_results <- c()
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
append(norm_results, shapiro.test(finches[,i]))
}
print(norm_results)
norm_results <- c()
for (i in quant_col){
print(colnames(finches[i]))
hist(finches[,i], main=colnames(finches[i]))
print(shapiro.test(finches[,i]))
}
